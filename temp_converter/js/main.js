// When the user clicks on the "F to C" button 
$('#fahrenheit_to_celsius').on('click', function () {
	// Get the value of #temperature and store it in a new varible called userTemp

	// convert the varible userTemp to the datatye number (parseFloat)

	//actually convert the tempurature from Farenheit to Celcius, and store it in a new varible called Celcuis

	/*display the tempurature that the user inputted in Celcius as #result
		on the left it will display userTemp
		on the right it will display the converyed tempurature
	*/
});